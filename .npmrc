# 注意：strict-peer-dependencies=false 会掩盖 vite / vitest 之间的版本冲突
# （vitest ≥4 需要 vite 6+，而 vite 被 uni 锁定在 5.2.8）。
# 升级依赖后务必实跑一次 pnpm test，不要只看安装有没有报错。
strict-peer-dependencies=false
auto-install-peers=true
shamefully-hoist=true
